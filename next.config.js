/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/Portfolio1',
  assetPrefix: '/Portfolio1',
  output: 'export',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
